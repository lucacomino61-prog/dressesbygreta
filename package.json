{
  "name": "dressesbygreta",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3640 --strictPort",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 3641 --strictPort",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "gsap": "^3.13.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "wrangler": "^4.132.0"
  }
}
